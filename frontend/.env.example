# Frontend settings are read at BUILD time and end up in the public JavaScript bundle: never put
# secrets here. Local development needs no frontend env file (Vite proxies /api to the backend).
# Deployed: set VITE_API_URL in Vercel (Settings → Environment Variables) to the RENDER backend URL.
VITE_API_URL=https://your-backend.onrender.com
